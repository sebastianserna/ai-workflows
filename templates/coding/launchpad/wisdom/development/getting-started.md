# Getting started

## Prerequisites

<!-- What a developer (or agent) needs installed to work. Example:
- Node.js >= 20
- Docker (for local database)
- Repository access
-->

## Local setup

<!-- Steps to set up the project from scratch. Example:
1. Clone the repo
2. `cp .env.example .env` and configure variables
3. `npm install`
4. `npm run dev`
-->

## Code conventions

<!-- Rules the whole team follows. Example:
- Indentation: 2 spaces
- Commits: Conventional Commits (`feat:`, `fix:`, `docs:`)
- Branches: `feature/`, `fix/`, `hotfix/` + description
-->

## Testing

<!-- How tests are run and what's expected to be covered. Example:
- Framework: Vitest / Jest / pytest
- Run: `npm run test`
- Minimum coverage: all business logic
-->
