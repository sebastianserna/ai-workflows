# Guida introduttiva

## Prerequisiti

<!-- Cosa deve avere installato uno sviluppatore (o agente) per lavorare. Esempio:
- Node.js >= 20
- Docker (per il database locale)
- Accesso al repository
-->

## Setup locale

<!-- Passi per configurare il progetto da zero. Esempio:
1. Clonare il repository
2. `cp .env.example .env` e configurare le variabili
3. `npm install`
4. `npm run dev`
-->

## Convenzioni di codice

<!-- Regole che tutto il team segue. Esempio:
- Indentazione: 2 spazi
- Commit: Conventional Commits (`feat:`, `fix:`, `docs:`)
- Branch: `feature/`, `fix/`, `hotfix/` + descrizione
-->

## Testing

<!-- Come vengono eseguiti i test e cosa ci si aspetta sia coperto. Esempio:
- Framework: Vitest / Jest / pytest
- Esecuzione: `npm run test`
- Copertura minima: tutta la logica di business
-->
