# Primeiros passos

## Pré-requisitos

<!-- O que um desenvolvedor (ou agente) precisa ter instalado para trabalhar. Exemplo:
- Node.js >= 20
- Docker (para banco de dados local)
- Acesso ao repositório
-->

## Setup local

<!-- Passos para configurar o projeto do zero. Exemplo:
1. Clonar o repositório
2. `cp .env.example .env` e configurar as variáveis
3. `npm install`
4. `npm run dev`
-->

## Convenções de código

<!-- Regras que toda a equipe segue. Exemplo:
- Indentação: 2 espaços
- Commits: Conventional Commits (`feat:`, `fix:`, `docs:`)
- Branches: `feature/`, `fix/`, `hotfix/` + descrição
-->

## Testes

<!-- Como os testes são executados e o que deve ser coberto. Exemplo:
- Framework: Vitest / Jest / pytest
- Executar: `npm run test`
- Cobertura mínima: toda a lógica de negócios
-->
