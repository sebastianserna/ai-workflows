# Pour commencer

## Prerequis

<!-- Ce qu'un developpeur (ou agent) doit avoir installe pour travailler. Exemple :
- Node.js >= 20
- Docker (pour la base de donnees locale)
- Acces au depot
-->

## Setup local

<!-- Etapes pour mettre en place le projet depuis zero. Exemple :
1. Cloner le repo
2. `cp .env.example .env` et configurer les variables
3. `npm install`
4. `npm run dev`
-->

## Conventions de code

<!-- Regles suivies par toute l'equipe. Exemple :
- Indentation : 2 espaces
- Commits : Conventional Commits (`feat:`, `fix:`, `docs:`)
- Branches : `feature/`, `fix/`, `hotfix/` + description
-->

## Testing

<!-- Comment les tests sont executes et ce qui est attendu en couverture. Exemple :
- Framework : Vitest / Jest / pytest
- Executer : `npm run test`
- Couverture minimale : toute la logique metier
-->
